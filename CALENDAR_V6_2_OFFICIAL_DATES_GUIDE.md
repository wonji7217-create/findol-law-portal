# findol v6.2 — 공식 날짜 기준 개정 캘린더

## 왜 수정했나
기존 개발 버전에서는 일부 자료에 공식 게시일이 없을 때 findol이 자료를 가져온 날짜가 `published_date`로 대체될 수 있었습니다. 그 결과 같은 날 동기화한 여러 게시물이 캘린더의 같은 날짜에 몰려 보일 수 있었습니다.

v6.2부터 **findol 수집일(collected_at)은 캘린더 일정 날짜로 절대 사용하지 않습니다.**

## 캘린더 날짜 기준

### 국민참여입법센터
- 행정예고/입법예고: API의 공고일(`pntcDt`)을 우선 사용
- 공고일이 없는 경우: 예고 시작일(`stYd`) 사용
- 의견제출 마감: 예고 종료일(`edYd`) 사용
- findol이 동기화한 날짜: 상세/관리용 메타데이터로만 보관

### 국가법령정보센터(법제처 Open API)
- 법령: 공포일자 → `공포`, 시행일자 → `시행`
- 행정규칙/고시: 발령일자 → `발령`
- 사용자가 규정검색을 실행한 날짜는 캘린더에 사용하지 않음

## 검색과 캘린더의 분리
- 규정검색: 필요한 법령을 찾기 위한 기능
- 개정 아카이브: 공식 자료의 날짜 이벤트를 이력으로 보여주는 기능
- 개정 캘린더: 공식 출처의 공고·예고·공포·발령·시행·의견마감 날짜만 표시

검색어를 입력하거나 검색을 실행한 날짜가 캘린더 일정으로 생성되지 않습니다.

## 관리자 동기화

### 1. 국민참여입법센터 동기화
기존 관리자 화면의 `새 정보 가져오기`를 사용합니다.

필요 환경변수:
`LAWMAKING_API_OC`

### 2. 법제처 공식 일정 동기화
관리자 화면에 `법제처 공식 일정 동기화` 영역이 추가되었습니다.

`법령·고시 정리`에 등록된 23개 고유 규정을 기준으로 국가법령정보센터 Open API를 조회하여 공포·발령·시행일을 저장합니다.

필요 환경변수:
`LAW_API_OC`

검색어에 따라 대상이 바뀌는 것이 아니라, findol의 법령·고시 정리표가 기준입니다.

## 화면 표시
각 일정에는 다음 정보가 함께 표시됩니다.
- 일정 유형
- 공식 날짜
- 규정명
- 소관기관
- 출처(국민참여입법센터 / 국가법령정보센터)
- 날짜 근거(공고일·예고일 / 의견제출 종료일 / 공포·발령일 / 시행일)

## 기존 잘못 표시된 데이터
v6.2 배포 후 개발 DB에서 국민참여입법센터 동기화를 다시 실행하면 동일 `source_key`의 최신 버전이 공식 날짜 기준으로 갱신됩니다.

개발 중 SQLite가 초기화되는 경우에는 배포 후 아래 순서로 다시 채우면 됩니다.
1. 국민참여입법센터 `새 정보 가져오기`
2. 법제처 `공식 일정 가져오기`
3. 개정 캘린더 확인

## 변경 파일
- `backend/app/storage.py`
- `backend/app/law_api.py`
- `backend/app/main.py`
- `frontend/index.html`
- `frontend/script.js`
- `frontend/admin.html`
- `frontend/admin.js`

## 확인
- Python 문법 검사 통과
- JavaScript 문법 검사 통과
- 기존 backend 테스트 17개 통과
