# findol v6.3 행정규칙 시행일 수집 보완

## 수정 이유
v6.2에서는 국가법령정보센터 행정규칙 목록 API의 `발령일자`는 저장했지만, 응답에 포함된 `시행일자`를 정규화 단계에서 누락하여 고시 상세 화면과 개정 캘린더에 시행일이 비어 보일 수 있었습니다.

## 수정 내용
- 행정규칙 목록 응답의 `시행일자`를 `enforcement_date`로 함께 저장합니다.
- 따라서 고시·훈령·예규도 발령일과 시행일을 각각 표시할 수 있습니다.
- 캘린더에는 발령일은 `공포·발령`, 시행일은 `시행` 이벤트로 각각 표시됩니다.
- 법령은 기존과 같이 `공포일자`와 `시행일자`를 함께 사용합니다.

## 적용 후
관리자에서 `법제처 공식 일정 가져오기`를 다시 실행해야 기존 DB 항목에 시행일이 채워집니다. 개발 중 SQLite가 초기화된 경우에는 동기화 1회만 다시 실행하면 됩니다.
