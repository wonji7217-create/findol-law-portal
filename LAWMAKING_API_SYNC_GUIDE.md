# findol v5.9 국민참여입법센터 API 연결 안내

## 1. 이번 버전에서 추가된 기능

- 국민참여입법센터 행정예고 목록 API 연결
- 행정예고 상세 본문 및 첨부파일 목록 수집
- 국민참여입법센터 입법예고 목록·상세 API 연결 구조 추가
- 수집 결과를 findol 개정 아카이브에 중복 없이 저장
- 의견제출 종료일을 개정 캘린더와 홈의 `다가오는 일정`에 표시
- 관리자 페이지에 `국민참여입법센터 동기화` 버튼 추가
- API 인증값이 공개 URL이나 프론트엔드에 노출되지 않도록 백엔드 환경변수로만 처리

## 2. Render 환경변수 등록

Render의 findol 서비스에서 다음 순서로 이동합니다.

1. Dashboard
2. `findol-law-portal` 서비스 선택
3. Environment
4. Add Environment Variable

아래 값을 등록합니다.

```text
Key: LAWMAKING_API_OC
Value: 정보공개 서비스 신청 ID의 @ 앞부분
```

예를 들어 신청 ID가 `example@email.com`이라면 Value에는 `example`만 입력합니다.

API 값을 GitHub 코드, HTML, JavaScript에 직접 입력하면 안 됩니다.

입법예고 소관부처 코드는 기본적으로 다음 값이 적용됩니다.

```text
LAWMAKING_MINISTRY_CODE=1480000
```

## 3. 관리자에서 자료 가져오기

1. 사이트 오른쪽 위 `관리자` 선택
2. 관리자 토큰 입력
3. `국민참여입법센터 동기화` 영역 확인
4. `행정예고`, `입법예고` 선택
5. `새 정보 가져오기` 선택

결과는 다음과 같이 표시됩니다.

```text
확인 24건 · 신규/변경 7건 · 기존과 동일 17건
```

- 신규/변경: 개정 아카이브에 새로 저장된 자료
- 기존과 동일: 이전에 저장한 내용과 같아서 중복 저장하지 않은 자료

## 4. 저장되는 정보

- 예고 제목
- 기관명
- 고시·공고·법률·부령 등의 유형
- 제정·일부개정 등의 형식
- 예고 시작일과 종료일
- 담당 전화·팩스·이메일
- 상세 본문
- 행정규칙안·법령안 파일
- 일반 첨부파일

상세 API 주소에는 인증값이 필요하므로, 아카이브의 공식 사이트 링크에는 인증값이 없는 국민참여입법센터 홈페이지 주소만 저장합니다.

## 5. 현재 자동화 범위

이번 v5.9는 관리자 버튼으로 실행하는 1차 동기화 버전입니다.

자동으로 수행되는 작업:

```text
목록 조회
→ 관련 자료 선별
→ 상세 API 조회
→ 본문과 첨부파일 링크 정리
→ 기존 자료와 비교
→ 개정 아카이브 저장
→ 의견제출 마감일 표시
```

아직 포함하지 않은 작업:

- 매일 정해진 시간에 자동 실행하는 스케줄러
- HWPX·PDF·Excel 첨부파일 내부의 물질명과 CAS 번호 자동 추출
- 추출된 물질을 물질검색의 `행정예고·지정예정` 상태와 자동 연결

먼저 API 수집 결과와 관련 자료 선별 정확도를 확인한 뒤, 다음 단계에서 일일 자동 실행과 첨부파일 분석을 연결하는 것이 안전합니다.

## 6. 확인용 테스트

배포 후 관리자에서 동기화를 실행하고 다음 내용을 확인합니다.

- 홈 `최근 개정정보`에 입법예고·행정예고가 표시되는지
- 홈 `다가오는 일정`에 의견마감 D-day가 표시되는지
- 개정 아카이브에서 제목과 기관명으로 검색되는지
- 상세창에서 본문과 첨부파일 링크가 표시되는지
- 같은 자료를 다시 동기화했을 때 중복으로 늘어나지 않는지
