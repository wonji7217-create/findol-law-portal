# findol v6.4 — 공포·발령/시행 동일일 결합 표시

## 변경 내용

법령·행정규칙의 공포(또는 발령)일과 시행일이 같은 날짜이면 캘린더와 개정 아카이브에서 두 건으로 중복 표시하지 않고 하나로 묶습니다.

- 법령: `공포·시행`
- 행정규칙(고시·훈령·예규): `발령·시행`

예: 2026.06.16에 발령과 시행이 동시에 이루어진 고시는 `발령·시행` 1건으로 표시합니다.

상세 화면에서도 날짜 카드가 `발령일` + `시행일` 두 칸이 아니라 `발령·시행일` 한 칸으로 합쳐집니다.

필터에서 공포·발령 또는 시행만 선택한 경우에는 해당 관점의 단일 이벤트로 볼 수 있고, 둘 다 선택된 기본 상태에서는 결합 이벤트가 표시됩니다.

결합 배지는 공포·발령의 주황색과 시행의 녹색을 반반 사용합니다.
