# findol v4 관리자 페이지 사용 안내

## 1. 관리자 페이지 주소

로컬 실행 시:

- `http://127.0.0.1:8000/admin.html`

실제 도메인 적용 후:

- `https://www.findol.me.kr/admin.html`

## 2. 관리자 토큰 설정

### 컴퓨터의 `backend/.env`

```env
ADMIN_TOKEN=본인만아는_길고_복잡한_문자열
```

### Render

`findol-law-portal → Environment → Add Environment Variable`

- Key: `ADMIN_TOKEN`
- Value: 컴퓨터 `.env`와 동일한 값

토큰은 GitHub, 캡처 화면, 메신저에 공개하지 않습니다.

## 3. 관리자 페이지에서 할 수 있는 일

- 환경지식 주제 추가·수정·삭제
- 대표 검색어 및 확장 검색어 관리
- 핵심 적용 고시 연결
- 상위 법령 및 함께 확인할 규정 연결
- 검색 의도 안내문 관리
- 먼저 확인할 사항과 관련 업무 관리
- 주제 사용/숨김 전환
- 검색 우선순위 조정

저장한 내용은 검색 API에 즉시 반영됩니다.

## 4. 주제 예시

- 표시명: 저장시설
- 주제 ID: `storage_facility`
- 대표 검색어: 저장시설, 저장탱크, 약품탱크, 폐액탱크
- 핵심 적용 규정: 유해화학물질 제조·사용·저장시설 설치 및 관리에 관한 고시
- 먼저 확인할 사항: 저장용량, 취급물질, 설치위치, 검사대상 여부

## 5. 중요한 저장 주의사항

기본 SQLite는 Render 무료 서버 재시작·재배포 시 데이터가 사라질 수 있습니다.
관리자 페이지를 실제 지식 DB로 장기간 사용하려면 `DATABASE_URL`에 Supabase/PostgreSQL 연결값을 등록해야 합니다.

## 6. 현재 버전 범위

v4 관리자 페이지는 우선 `환경지식 DB` 관리에 집중합니다. 앞으로 같은 구조에 아래 메뉴를 추가할 수 있습니다.

- 워크플로우
- 체크리스트
- 계산기
- 실무사례
- 개정정보 수동 검토·승인
